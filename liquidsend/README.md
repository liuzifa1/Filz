# Notes
## this is just notes of my idea, just ignore it
1. [App framework] this app is solely made for file transfer between ios & other platform, thus ALL the UI/feature should be native, except localsend core.
2. [App UI] For app's user, they will only reaching this app when they need to send file, recieve file, check the file history. thus content view should only feature 3 main tab, "Home", "History", "Send files". Because the recieve only request confirm (even not), so the most important feature sould be "send", so it will holds .search role. Settings is kind of function u only need to dealt once, so it will apear as a toolbaritem. Home & history will act as a tab item.
    2.1. [Send button] Because the ios file system design, sending photo & files need two dedicated entry, thus send button will open a .sheet, within this page u can configure the destination, file type, send mode etc.
